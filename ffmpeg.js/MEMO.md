 - `Makefile`: ffmpeg.js custom build with this 
 - `plotdb-ffmpeg-worker.js`: built worker js supporting webm, mp4 and animated webp
 - also add submodule for `ffmpeg-plotdb` and `libwebp` as described in `/README.md`
